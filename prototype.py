from _generate_charts import generate_charts
from _generate_data import calculate
from _unautomated_methods import (call_wrappers, concentration_effects_wrapper,
                                  grouped_separation_factor_histogram_wrapper,
                                  histogram_wrapper)
from projects.nd_sm_cut import projects

generate_charts(False)

# grouped_separation_factor_histogram_wrapper()

# concentration_effects_wrapper()

# histogram_wrapper()
